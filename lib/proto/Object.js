/**
 * Executes a function for each key/value in the object.
 *
 * @param {Function} fn The function to execute fn(key, value, index, object)
 */
//Object.prototype.forEach = function forEach(fn) {
//	var self = this;
//	Object.keys(this).forEach(function (key, index, array) {
//		fn(key, this[key], index, self);
//	});
//};

//String.prototype.map = function map(fn) {
//	var self = this;
//	Object.keys(this).map(function (key, index, array) {
//		fn(key, this[key], index, self);
//	});
//};
